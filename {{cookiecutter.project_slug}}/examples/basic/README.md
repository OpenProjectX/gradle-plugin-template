# Basic {{ cookiecutter.plugin_name }} Example

This example applies the plugin from the repository checkout via `includeBuild("../..")`.

```bash
env GRADLE_USER_HOME=/data/.gradle ../../gradlew sayHello --no-configuration-cache
```
